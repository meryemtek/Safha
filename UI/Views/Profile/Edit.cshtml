@model UI.Models.ProfileEditViewModel

@{
    ViewData["Title"] = "Profil Düzenle";
    Layout = "_Layout";
}

<div class="profile-edit-container">
    <div class="edit-header">
        <h1>✏️ Profil Düzenle</h1>
        <p>Profil bilgilerinizi güncelleyin ve fotoğraflarınızı değiştirin</p>
    </div>

    <div class="edit-form-container">
        <form asp-action="Edit" method="post" enctype="multipart/form-data" class="edit-form">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            
            <div class="photo-upload-section">
                <div class="upload-card">
                    <h3>📷 Fotoğraflar</h3>
                    
                    <div class="cover-photo-upload">
                        <label class="upload-label-text">Kapak Fotoğrafı</label>
                        <div class="cover-photo-preview" id="coverPhotoPreview" style="@(!string.IsNullOrEmpty(Model.CurrentCoverPhoto) ? $"background-image: url('{Model.CurrentCoverPhoto}')" : "")" onclick="document.getElementById('coverPhotoInput').click();">
                            <input type="file" name="CoverPhotoFile" id="coverPhotoInput" accept="image/*" style="display: none;" />
                            <div class="upload-overlay">
                                <i class="icon">📷</i>
                                <span>@(!string.IsNullOrEmpty(Model.CurrentCoverPhoto) ? "Fotoğrafı değiştir" : "Fotoğraf ekle")</span>
                            </div>
                        </div>
                        <small>Önerilen boyut: 1200x300 piksel</small>
                    </div>
                    
                    <div class="profile-photo-upload">
                        <label class="upload-label-text">Profil Fotoğrafı</label>
                        <div class="profile-photo-preview" id="profilePhotoPreview" style="@(!string.IsNullOrEmpty(Model.CurrentProfilePicture) ? $"background-image: url('{Model.CurrentProfilePicture}')" : "")" onclick="document.getElementById('profilePhotoInput').click();">
                            <input type="file" name="ProfilePictureFile" id="profilePhotoInput" accept="image/*" style="display: none;" />
                            <div class="upload-overlay">
                                <i class="icon">👤</i>
                                <span>@(!string.IsNullOrEmpty(Model.CurrentProfilePicture) ? "Fotoğrafı değiştir" : "Fotoğraf ekle")</span>
                            </div>
                        </div>
                        <small>Önerilen boyut: 400x400 piksel</small>
                    </div>
                </div>
            </div>
            
            <div class="form-sections">
                <div class="form-section">
                    <h3>👤 Kişisel Bilgiler</h3>
                    
                    <div class="form-row">
                        <div class="form-group">
                            <label asp-for="FirstName">Ad</label>
                            <input asp-for="FirstName" class="form-control" placeholder="Adınızı girin" />
                            <span asp-validation-for="FirstName" class="text-danger"></span>
                        </div>
                        
                        <div class="form-group">
                            <label asp-for="LastName">Soyad</label>
                            <input asp-for="LastName" class="form-control" placeholder="Soyadınızı girin" />
                            <span asp-validation-for="LastName" class="text-danger"></span>
                        </div>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="Bio">Hakkımda</label>
                        <textarea asp-for="Bio" class="form-control" rows="4" maxlength="500" 
                                  placeholder="Kendiniz hakkında kısa bir açıklama yazın..."></textarea>
                        <div class="char-counter">
                            <span id="bioCharCount">0</span>/500
                        </div>
                        <span asp-validation-for="Bio" class="text-danger"></span>
                    </div>
                </div>
                
                <div class="form-section">
                    <h3>📚 Okuma Hedefleri</h3>
                    
                    <div class="form-group">
                        <label asp-for="TargetBookCount">Yıllık Okuma Hedefi</label>
                        <input asp-for="TargetBookCount" type="number" class="form-control" 
                               min="1" max="1000" placeholder="Hedef kitap sayısı" />
                        <small>Bu yıl kaç kitap okumayı hedefliyorsunuz?</small>
                        <span asp-validation-for="TargetBookCount" class="text-danger"></span>
                    </div>
                </div>
                
                <div class="form-section">
                    <h3>📞 İletişim Bilgileri</h3>
                    
                    <div class="form-group">
                        <label asp-for="PhoneNumber">Telefon Numarası</label>
                        <input asp-for="PhoneNumber" class="form-control" placeholder="+90 5XX XXX XX XX" />
                        <span asp-validation-for="PhoneNumber" class="text-danger"></span>
                    </div>
                    
                    <div class="form-group">
                        <label asp-for="Address">Adres</label>
                        <textarea asp-for="Address" class="form-control" rows="3" 
                                  placeholder="Adres bilgilerinizi girin"></textarea>
                        <span asp-validation-for="Address" class="text-danger"></span>
                    </div>
                </div>
            </div>
            
            <div class="form-actions">
                <a href="@Url.Action("Index", "Profile")" class="btn btn-cancel">
                    <i class="icon">❌</i> İptal
                </a>
                <button type="submit" class="btn btn-save">
                    <i class="icon">💾</i> Kaydet
                </button>
            </div>
        </form>
    </div>
</div>

@section Styles {
    <link rel="stylesheet" href="~/css/profile-edit.css" />
}

@section Scripts {
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const editForm = document.querySelector('.edit-form');
            const profileInput = document.getElementById('profilePhotoInput');
            const profilePreview = document.getElementById('profilePhotoPreview');
            const coverInput = document.getElementById('coverPhotoInput');
            const coverPreview = document.getElementById('coverPhotoPreview');
            const bioTextarea = document.getElementById('Bio');
            const bioCharCount = document.getElementById('bioCharCount');
            const submitButton = document.querySelector('button[type="submit"]');
            
            // Profil fotoğrafı önizleme ve validasyon
            if (profileInput && profilePreview) {
                console.log('Profil fotoğrafı input ve preview bulundu');
                
                // Label'a tıklama event'i ekle (ekstra güvenlik için)
                if (profilePreview.tagName === 'LABEL') {
                    profilePreview.addEventListener('click', function(e) {
                        console.log('Profil fotoğrafı label tıklandı');
                        // Label otomatik olarak input'u tetikler, ama manuel de tetikleyelim
                        if (profileInput) {
                            setTimeout(function() {
                                profileInput.click();
                            }, 10);
                        }
                    });
                }
                
                profileInput.addEventListener('change', function() {
                    if (this.files && this.files[0]) {
                        const file = this.files[0];
                        
                        // Dosya tipi kontrolü
                        if (!file.type.match('image.*')) {
                            Swal.fire({
                                icon: 'error',
                                title: 'Hata!',
                                text: 'Lütfen geçerli bir resim dosyası seçin (JPG, PNG, GIF)'
                            });
                            this.value = '';
                            return;
                        }
                        
                        // Dosya boyutu kontrolü (5MB)
                        if (file.size > 5 * 1024 * 1024) {
                            Swal.fire({
                                icon: 'error',
                                title: 'Hata!',
                                text: 'Dosya boyutu 5MB\'dan küçük olmalıdır'
                            });
                            this.value = '';
                            return;
                        }
                        
                        const reader = new FileReader();
                        reader.onload = function(e) {
                            profilePreview.style.backgroundImage = `url(${e.target.result})`;
                            profilePreview.classList.add('has-image');
                        }
                        reader.readAsDataURL(file);
                    }
                });
            }
            
            // Kapak fotoğrafı önizleme ve validasyon
            if (coverInput && coverPreview) {
                console.log('Kapak fotoğrafı input ve preview bulundu');
                
                // Label'a tıklama event'i ekle (ekstra güvenlik için)
                if (coverPreview.tagName === 'LABEL') {
                    coverPreview.addEventListener('click', function(e) {
                        console.log('Kapak fotoğrafı label tıklandı');
                        // Label otomatik olarak input'u tetikler, ama manuel de tetikleyelim
                        if (coverInput) {
                            setTimeout(function() {
                                coverInput.click();
                            }, 10);
                        }
                    });
                }
                
                coverInput.addEventListener('change', function() {
                    if (this.files && this.files[0]) {
                        const file = this.files[0];
                        
                        // Dosya tipi kontrolü
                        if (!file.type.match('image.*')) {
                            Swal.fire({
                                icon: 'error',
                                title: 'Hata!',
                                text: 'Lütfen geçerli bir resim dosyası seçin (JPG, PNG, GIF)'
                            });
                            this.value = '';
                            return;
                        }
                        
                        // Dosya boyutu kontrolü (5MB)
                        if (file.size > 5 * 1024 * 1024) {
                            Swal.fire({
                                icon: 'error',
                                title: 'Hata!',
                                text: 'Dosya boyutu 5MB\'dan küçük olmalıdır'
                            });
                            this.value = '';
                            return;
                        }
                        
                        const reader = new FileReader();
                        reader.onload = function(e) {
                            coverPreview.style.backgroundImage = `url(${e.target.result})`;
                            coverPreview.classList.add('has-image');
                        }
                        reader.readAsDataURL(file);
                    }
                });
            }
            
            // Biyografi karakter sayacı
            if (bioTextarea && bioCharCount) {
                bioTextarea.addEventListener('input', function() {
                    const currentLength = this.value.length;
                    bioCharCount.textContent = currentLength;
                    
                    if (currentLength > 450) {
                        bioCharCount.classList.add('text-warning');
                    } else {
                        bioCharCount.classList.remove('text-warning');
                    }
                });
                
                // Sayfa yüklendiğinde mevcut biyografi uzunluğunu göster
                if (bioTextarea.value) {
                    bioCharCount.textContent = bioTextarea.value.length;
                }
            }
            
            // Form submit işlemi
            if (editForm) {
                editForm.addEventListener('submit', function(e) {
                    // Form validasyonu
                    const firstName = document.querySelector('input[name="FirstName"]');
                    const lastName = document.querySelector('input[name="LastName"]');
                    
                    if (!firstName || !firstName.value.trim()) {
                        e.preventDefault();
                        Swal.fire({
                            icon: 'error',
                            title: 'Hata!',
                            text: 'Ad alanı zorunludur'
                        });
                        firstName.focus();
                        return false;
                    }
                    
                    if (!lastName || !lastName.value.trim()) {
                        e.preventDefault();
                        Swal.fire({
                            icon: 'error',
                            title: 'Hata!',
                            text: 'Soyad alanı zorunludur'
                        });
                        lastName.focus();
                        return false;
                    }
                    
                    // Submit butonunu devre dışı bırak ve loading göster
                    if (submitButton) {
                        submitButton.disabled = true;
                        const originalText = submitButton.innerHTML;
                        submitButton.innerHTML = '<i class="icon">⏳</i> Kaydediliyor...';
                        
                        // Form gönderildikten sonra butonu tekrar aktif et (eğer hata olursa)
                        setTimeout(function() {
                            submitButton.disabled = false;
                            submitButton.innerHTML = originalText;
                        }, 10000);
                    }
                });
            }
            
            // Başarı mesajını göster (TempData'dan)
            @if (TempData["SuccessMessage"] != null)
            {
                <text>
                Swal.fire({
                    icon: 'success',
                    title: 'Başarılı!',
                    text: '@TempData["SuccessMessage"]',
                    timer: 3000,
                    showConfirmButton: false
                });
                </text>
            }
        });
    </script>
}